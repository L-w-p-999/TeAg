<!-- BEGIN:nextjs-agent-rules -->
# 这不是你熟悉的 Next.js

此版本有重大更改 — API、约定和文件结构可能与您的训练数据有所不同。在编写代码之前，请阅读 `node_modules/next/dist/docs/` 中的相关指南，并注意废弃通知。

### 使用 pnpm 下载远程包

为了提高项目管理效率，可以使用 `pnpm` 来下载和管理远程包。以下是常用的 `pnpm` 命令：

#### 1. 安装 pnpm

首先，确保你已安装 `pnpm`，如果没有安装，可以通过以下命令进行安装：

```bash
npm install -g pnpm
<!-- END:nextjs-agent-rules -->
